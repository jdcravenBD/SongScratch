/**
 * Foundation placeholder.
 *
 * This shell exists only to confirm the toolchain, the black/white/grey theme
 * and iPhone testing all work end to end. The real screens — the song list and
 * the three per-save tabs (Chords, Lyrics, Voice) — are built in later steps.
 */
export default function App() {
  return (
    <main className="app">
      <h1 className="wordmark">Song Scratch</h1>
      <p className="tagline">Chords, lyrics &amp; voice notes — one scratch at a time.</p>
      <p className="scaffold-note">Foundation ready. Screens coming next.</p>
    </main>
  );
}
