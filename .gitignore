node_modules/
dist/
dist-ssr/
*.local
.DS_Store
Thumbs.db

# editors
.vscode/*
!.vscode/extensions.json
.idea/

# Android has no native project yet; iOS is committed on purpose. A build
# machine that regenerated ios/ would lose every Info.plist edit with it and
# ship an app that dies on its first permission request.
/android
